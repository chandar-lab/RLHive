name: Python unit tests for Hive

on:
  pull_request:
    branches:
      - dev 

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 5

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.8
      uses: actions/setup-python@v2
      with:
        python-version: 3.8

    - name: Add conda to system path
      run: |
        # $CONDA is an environment variable pointing to the root of the miniconda directory
        echo $CONDA/bin >> $GITHUB_PATH && echo "CONDA=$CONDA" >> $GITHUB_ENV
        sudo chmod -R a+rw $CONDA/lib/python3.8/
    - name: Cache python environment
      env:
        cache_version: 2
      uses: actions/cache@v2
      with:
        # conda cache files are stored in `$CONDA/pkgs` on Linux/macOS
        path: ${{ env.CONDA }}/lib/python3.8/
        key: v${{ env.cache_version }}-${{ runner.os }}-conda-${{ hashFiles('environment.yml') }}
        restore-keys: |
          v${{ env.cache_version }}-${{ runner.os }}-conda-
          v${{ env.cache_version }}-${{ runner.os }}-

    - name: Install dependencies
      run: |
        conda env update --file environment.yml --name base

    - name: Test with pytest
      run: |
        python -m pytest
